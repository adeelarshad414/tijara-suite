# Screenshot Index

Run the generated capture script against a live local or staging Tijara instance:

```bash
node scripts/capture-screenshots.js
```

The script writes full-page screenshots by persona into this directory and
refreshes this index with inline image links.
